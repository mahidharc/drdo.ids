def deploy(stream, InputTraffic, awc):
  #do the initialization

a=0.0
for i in range(windowCount):
  for j in range(0,6)
  p=determineProbability(windowCount, stream)
  if ( p < stream.thresholdProbability):
    a+=1
  else:
    a-=1
  if a>=awc:
    return attack


